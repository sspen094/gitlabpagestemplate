# =============================================================================
# DEPLOYED CONFIG — TST (mfgcidashboard)
# Same workflow as DEV; Install-ProjectEnv.ps1 -Environment tst
#
# TST POLICY (typical):
#   - Confirm SQL + runtime hostnames with IT before FILL
#   - Set integration *_POLL_ENABLED=false unless QA needs them
#   - Harden secrets before exposing TST outside the team
#
# See: deployments/env/README.md · docs/developer.md §7
# =============================================================================

APP_ENV=tst
APP_NAME=MFGCIDashboard

# --- SQL (FILL) ---
APP_DB_HOST=
APP_DB_PORT=1433
APP_DB_DOMAIN=
APP_DB_USER=
APP_DB_PASSWORD=
APP_DB_NAME=CIDashboard

# --- App URLs (FILL) ---
APP_SERVER_TST_PUBLIC_URL=

# --- ClickUp (OPTIONAL) ---
# CLICKUP_POLL_ENABLED=false

# Copy remaining sections from project.dev.env and adjust hosts/flags.
