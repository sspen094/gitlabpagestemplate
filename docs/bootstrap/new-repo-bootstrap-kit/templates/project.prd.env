# =============================================================================
# DEPLOYED CONFIG — PRD (mfgcidashboard)
# Install-ProjectEnv.ps1 -Environment prd
#
# PRD POLICY:
#   - Confirm all hostnames and accounts with IT / security before go-live
#   - Dedicated integration tokens (not shared with DEV)
#   - Enable *_POLL_ENABLED only after operational sign-off
#
# See: deployments/DEPLOY.md · docs/developer.md §7
# =============================================================================

APP_ENV=prd
APP_NAME=MFGCIDashboard

# --- SQL (FILL — production) ---
APP_DB_HOST=
APP_DB_PORT=1433
APP_DB_DOMAIN=
APP_DB_USER=
APP_DB_PASSWORD=
APP_DB_NAME=CIDashboard

# --- App URLs (FILL) ---
APP_SERVER_PRD_PUBLIC_URL=

# --- Integrations: enable deliberately in production ---
